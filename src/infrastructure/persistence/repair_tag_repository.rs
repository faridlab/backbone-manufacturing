//! Repository for RepairTag entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<RepairTag, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::RepairTag;

/// Table name for RepairTag entities
pub const TABLE_NAME: &str = "manufacturing.repair_tags";

/// Repository for RepairTag entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct RepairTagRepository(
    backbone_orm::GenericCrudRepository<RepairTag, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for RepairTagRepository {
    type Target = backbone_orm::GenericCrudRepository<RepairTag, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl RepairTagRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(RepairTagRepository, RepairTag, soft_delete);
