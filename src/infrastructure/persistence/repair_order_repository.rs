//! Repository for RepairOrder entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<RepairOrder, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::RepairOrder;

/// Table name for RepairOrder entities
pub const TABLE_NAME: &str = "manufacturing.repair_orders";

/// Repository for RepairOrder entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct RepairOrderRepository(
    backbone_orm::GenericCrudRepository<RepairOrder, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for RepairOrderRepository {
    type Target = backbone_orm::GenericCrudRepository<RepairOrder, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl RepairOrderRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(RepairOrderRepository, RepairOrder, soft_delete);
