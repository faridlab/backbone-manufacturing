//! Public types for Manufacturing module
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! These DTOs are the ONLY types other modules should use.
//! They are decoupled from internal domain entities.

use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};
use rust_decimal::Decimal;
use crate::domain::entity::*;

// ============================================================================
// WORKSTATION TYPES
// ============================================================================

/// Type-safe ID for Workstation
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct WorkstationId(pub Uuid);

impl WorkstationId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for WorkstationId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<WorkstationId> for Uuid {
    fn from(id: WorkstationId) -> Self {
        id.0
    }
}

/// Data transfer object for Workstation
///
/// This is the public representation of Workstation for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkstationDto {
    pub id: WorkstationId,
    pub company_id: Uuid,
    pub workstation_name: String,
    pub hour_rate: Decimal,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of Workstation for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkstationSummary {
    pub id: WorkstationId,
    pub workstation_name: String,
}

/// Reference to Workstation for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkstationRef {
    pub id: WorkstationId,
}

// ============================================================================
// OPERATION TYPES
// ============================================================================

/// Type-safe ID for Operation
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct OperationId(pub Uuid);

impl OperationId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for OperationId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<OperationId> for Uuid {
    fn from(id: OperationId) -> Self {
        id.0
    }
}

/// Data transfer object for Operation
///
/// This is the public representation of Operation for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OperationDto {
    pub id: OperationId,
    pub company_id: Uuid,
    pub operation_name: String,
    pub default_workstation_id: Option<Uuid>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of Operation for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OperationSummary {
    pub id: OperationId,
    pub operation_name: String,
}

/// Reference to Operation for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct OperationRef {
    pub id: OperationId,
}

// ============================================================================
// BOM TYPES
// ============================================================================

/// Type-safe ID for Bom
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct BomId(pub Uuid);

impl BomId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for BomId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<BomId> for Uuid {
    fn from(id: BomId) -> Self {
        id.0
    }
}

/// Data transfer object for Bom
///
/// This is the public representation of Bom for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomDto {
    pub id: BomId,
    pub company_id: Uuid,
    pub item_id: Uuid,
    pub bom_code: String,
    pub quantity: Decimal,
    pub uom: Option<String>,
    pub currency: String,
    pub raw_material_cost: Decimal,
    pub operating_cost: Decimal,
    pub total_cost: Decimal,
    pub is_active: bool,
    pub is_default: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of Bom for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomSummary {
    pub id: BomId,
}

/// Reference to Bom for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomRef {
    pub id: BomId,
}

// ============================================================================
// BOMITEM TYPES
// ============================================================================

/// Type-safe ID for BomItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct BomItemId(pub Uuid);

impl BomItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for BomItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<BomItemId> for Uuid {
    fn from(id: BomItemId) -> Self {
        id.0
    }
}

/// Data transfer object for BomItem
///
/// This is the public representation of BomItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomItemDto {
    pub id: BomItemId,
    pub bom_id: Uuid,
    pub item_id: Uuid,
    pub quantity: Decimal,
    pub rate: Decimal,
    pub amount: Decimal,
    pub is_phantom: bool,
    pub metadata: serde_json::Value,
}

/// Summary view of BomItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomItemSummary {
    pub id: BomItemId,
}

/// Reference to BomItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomItemRef {
    pub id: BomItemId,
}

// ============================================================================
// BOMOPERATION TYPES
// ============================================================================

/// Type-safe ID for BomOperation
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct BomOperationId(pub Uuid);

impl BomOperationId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for BomOperationId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<BomOperationId> for Uuid {
    fn from(id: BomOperationId) -> Self {
        id.0
    }
}

/// Data transfer object for BomOperation
///
/// This is the public representation of BomOperation for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomOperationDto {
    pub id: BomOperationId,
    pub bom_id: Uuid,
    pub operation_id: Uuid,
    pub workstation_id: Uuid,
    pub time_in_mins: Decimal,
    pub hour_rate: Decimal,
    pub operating_cost: Decimal,
    pub metadata: serde_json::Value,
}

/// Summary view of BomOperation for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomOperationSummary {
    pub id: BomOperationId,
}

/// Reference to BomOperation for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BomOperationRef {
    pub id: BomOperationId,
}

// ============================================================================
// WORKORDER TYPES
// ============================================================================

/// Type-safe ID for WorkOrder
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct WorkOrderId(pub Uuid);

impl WorkOrderId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for WorkOrderId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<WorkOrderId> for Uuid {
    fn from(id: WorkOrderId) -> Self {
        id.0
    }
}

/// Data transfer object for WorkOrder
///
/// This is the public representation of WorkOrder for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderDto {
    pub id: WorkOrderId,
    pub company_id: Uuid,
    pub work_order_number: String,
    pub item_id: Uuid,
    pub bom_id: Uuid,
    pub quantity: Decimal,
    pub produced_qty: Decimal,
    pub status: WorkOrderStatus,
    pub raw_material_cost: Decimal,
    pub operating_cost: Decimal,
    pub wip_warehouse_id: Option<Uuid>,
    pub fg_warehouse_id: Option<Uuid>,
    pub wip_account_id: Option<Uuid>,
    pub fg_account_id: Option<Uuid>,
    pub raw_material_account_id: Option<Uuid>,
    pub conversion_cost_account_id: Option<Uuid>,
    pub planned_start_date: Option<DateTime<Utc>>,
    pub metadata: serde_json::Value,
}

/// Summary view of WorkOrder for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderSummary {
    pub id: WorkOrderId,
    pub status: WorkOrderStatus,
}

/// Reference to WorkOrder for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderRef {
    pub id: WorkOrderId,
}

// ============================================================================
// WORKORDERITEM TYPES
// ============================================================================

/// Type-safe ID for WorkOrderItem
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct WorkOrderItemId(pub Uuid);

impl WorkOrderItemId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for WorkOrderItemId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<WorkOrderItemId> for Uuid {
    fn from(id: WorkOrderItemId) -> Self {
        id.0
    }
}

/// Data transfer object for WorkOrderItem
///
/// This is the public representation of WorkOrderItem for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderItemDto {
    pub id: WorkOrderItemId,
    pub work_order_id: Uuid,
    pub item_id: Uuid,
    pub required_qty: Decimal,
    pub consumed_qty: Decimal,
    pub rate: Decimal,
    pub metadata: serde_json::Value,
}

/// Summary view of WorkOrderItem for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderItemSummary {
    pub id: WorkOrderItemId,
}

/// Reference to WorkOrderItem for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct WorkOrderItemRef {
    pub id: WorkOrderItemId,
}

// ============================================================================
// JOBCARD TYPES
// ============================================================================

/// Type-safe ID for JobCard
///
/// Use this instead of raw Uuid for type safety across modules.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub struct JobCardId(pub Uuid);

impl JobCardId {
    pub fn new(id: Uuid) -> Self {
        Self(id)
    }

    pub fn into_inner(self) -> Uuid {
        self.0
    }
}

impl From<Uuid> for JobCardId {
    fn from(id: Uuid) -> Self {
        Self(id)
    }
}

impl From<JobCardId> for Uuid {
    fn from(id: JobCardId) -> Self {
        id.0
    }
}

/// Data transfer object for JobCard
///
/// This is the public representation of JobCard for other modules.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JobCardDto {
    pub id: JobCardId,
    pub company_id: Uuid,
    pub work_order_id: Uuid,
    pub operation_id: Uuid,
    pub workstation_id: Uuid,
    pub total_time_mins: Decimal,
    pub hour_rate: Decimal,
    pub operating_cost: Decimal,
    pub status: JobCardStatus,
    pub metadata: serde_json::Value,
}

/// Summary view of JobCard for list displays
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JobCardSummary {
    pub id: JobCardId,
    pub status: JobCardStatus,
}

/// Reference to JobCard for foreign key relationships
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct JobCardRef {
    pub id: JobCardId,
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

// <<< CUSTOM TYPES START >>>
// Add custom public types here
// <<< CUSTOM TYPES END >>>
