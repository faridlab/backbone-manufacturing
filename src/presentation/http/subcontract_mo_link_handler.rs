//! SubcontractMoLink REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{SubcontractMoLinkService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateSubcontractMoLinkDto, UpdateSubcontractMoLinkDto, PatchSubcontractMoLinkDto, SubcontractMoLinkResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum SubcontractMoLinkError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for SubcontractMoLinkError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for SubcontractMoLinkError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "SUBCONTRACTMOLINK_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "SUBCONTRACTMOLINK_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "SUBCONTRACTMOLINK_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "SUBCONTRACTMOLINK_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for SubcontractMoLink.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /subcontract_mo_links | List with pagination |
/// | POST | /subcontract_mo_links | Create new |
/// | GET | /subcontract_mo_links/:id | Get by ID |
/// | PUT | /subcontract_mo_links/:id | Full update |
/// | PATCH | /subcontract_mo_links/:id | Partial update |
/// | DELETE | /subcontract_mo_links/:id | Soft delete |
/// | POST | /subcontract_mo_links/bulk | Bulk create |
/// | POST | /subcontract_mo_links/upsert | Upsert |
/// | GET | /subcontract_mo_links/trash | List deleted |
/// | POST | /subcontract_mo_links/:id/restore | Restore |
/// | DELETE | /subcontract_mo_links/empty | Empty trash |
/// | GET | /subcontract_mo_links/:id/deleted | Get deleted by ID |
/// | DELETE | /subcontract_mo_links/trash/:id | Permanent delete from trash |
/// | GET | /subcontract_mo_links/count | Count active entities |
/// | GET | /subcontract_mo_links/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(SubcontractMoLinkService::with_repository(repository));
/// let router = create_subcontract_mo_link_routes(service);
/// ```
pub fn create_subcontract_mo_link_routes(service: Arc<SubcontractMoLinkService>) -> Router {
    BackboneCrudHandler::<SubcontractMoLinkService, SubcontractMoLink, CreateSubcontractMoLinkDto, UpdateSubcontractMoLinkDto, SubcontractMoLinkResponseDto>::routes(
        service,
        "/subcontract_mo_links",
    )
}

/// Create Axum router with only the read (GET) endpoints for SubcontractMoLink.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_subcontract_mo_link_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_subcontract_mo_link_read_routes(service: Arc<SubcontractMoLinkService>) -> Router {
    BackboneCrudHandler::<SubcontractMoLinkService, SubcontractMoLink, CreateSubcontractMoLinkDto, UpdateSubcontractMoLinkDto, SubcontractMoLinkResponseDto>::read_routes(
        service,
        "/subcontract_mo_links",
    )
}

/// Create Axum router with only the write (mutation) endpoints for SubcontractMoLink.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
///
/// # This is unguarded generic CRUD, not a validated write path
///
/// These are plain create/update/patch/delete mutations over the entity row —
/// they bypass all business invariants. If the module exposes a validated write
/// service (e.g. a command router over its domain engine), serve THAT instead
/// for any mutation that must respect domain rules.
pub fn create_subcontract_mo_link_write_routes(service: Arc<SubcontractMoLinkService>) -> Router {
    BackboneCrudHandler::<SubcontractMoLinkService, SubcontractMoLink, CreateSubcontractMoLinkDto, UpdateSubcontractMoLinkDto, SubcontractMoLinkResponseDto>::write_routes(
        service,
        "/subcontract_mo_links",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_subcontract_mo_link_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<SubcontractMoLinkService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_subcontract_mo_link_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}
