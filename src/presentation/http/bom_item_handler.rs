//! BomItem REST handlers
//!
//! Auto-generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{BomItemService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateBomItemDto, UpdateBomItemDto, PatchBomItemDto, BomItemResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum BomItemError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for BomItemError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for BomItemError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "BOMITEM_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "BOMITEM_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "BOMITEM_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "BOMITEM_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for BomItem.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /bom_items | List with pagination |
/// | POST | /bom_items | Create new |
/// | GET | /bom_items/:id | Get by ID |
/// | PUT | /bom_items/:id | Full update |
/// | PATCH | /bom_items/:id | Partial update |
/// | DELETE | /bom_items/:id | Soft delete |
/// | POST | /bom_items/bulk | Bulk create |
/// | POST | /bom_items/upsert | Upsert |
/// | GET | /bom_items/trash | List deleted |
/// | POST | /bom_items/:id/restore | Restore |
/// | DELETE | /bom_items/empty | Empty trash |
/// | GET | /bom_items/:id/deleted | Get deleted by ID |
/// | DELETE | /bom_items/trash/:id | Permanent delete from trash |
/// | GET | /bom_items/count | Count active entities |
/// | GET | /bom_items/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(BomItemService::with_repository(repository));
/// let router = create_bom_item_routes(service);
/// ```
pub fn create_bom_item_routes(service: Arc<BomItemService>) -> Router {
    BackboneCrudHandler::<BomItemService, BomItem, CreateBomItemDto, UpdateBomItemDto, BomItemResponseDto>::routes(
        service,
        "/bom_items",
    )
}

/// Create Axum router with only the read (GET) endpoints for BomItem.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_bom_item_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_bom_item_read_routes(service: Arc<BomItemService>) -> Router {
    BackboneCrudHandler::<BomItemService, BomItem, CreateBomItemDto, UpdateBomItemDto, BomItemResponseDto>::read_routes(
        service,
        "/bom_items",
    )
}

/// Create Axum router with only the write (mutation) endpoints for BomItem.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_bom_item_write_routes(service: Arc<BomItemService>) -> Router {
    BackboneCrudHandler::<BomItemService, BomItem, CreateBomItemDto, UpdateBomItemDto, BomItemResponseDto>::write_routes(
        service,
        "/bom_items",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_bom_item_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<BomItemService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_bom_item_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

