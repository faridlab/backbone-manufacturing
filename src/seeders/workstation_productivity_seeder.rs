//! WorkstationProductivity seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial WorkstationProductivity data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// WORKSTATIONPRODUCTIVITY SEEDER
// ============================================================================

/// Seeder for workstation_productivity
pub struct SeedWorkstationProductivitySeeder;

impl SeedWorkstationProductivitySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedWorkstationProductivitySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedWorkstationProductivitySeeder {
    fn name(&self) -> &'static str {
        "SeedWorkstationProductivitySeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if manufacturing.workstation_productivity table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM manufacturing.workstation_productivity")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/workstation_productivity_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM manufacturing.workstation_productivity")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
