//! RepairOrder seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RepairOrder data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REPAIRORDER SEEDER
// ============================================================================

/// Seeder for repair_order
pub struct SeedRepairOrderSeeder;

impl SeedRepairOrderSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRepairOrderSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRepairOrderSeeder {
    fn name(&self) -> &'static str {
        "SeedRepairOrderSeeder"
    }

    fn order(&self) -> i32 {
        11
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if manufacturing.repair_orders table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM manufacturing.repair_orders")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/repair_order_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM manufacturing.repair_orders")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
