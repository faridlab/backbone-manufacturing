//! RepairTag seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RepairTag data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REPAIRTAG SEEDER
// ============================================================================

/// Seeder for repair_tag
pub struct SeedRepairTagSeeder;

impl SeedRepairTagSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRepairTagSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRepairTagSeeder {
    fn name(&self) -> &'static str {
        "SeedRepairTagSeeder"
    }

    fn order(&self) -> i32 {
        13
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if manufacturing.repair_tags table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM manufacturing.repair_tags")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/repair_tag_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM manufacturing.repair_tags")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
