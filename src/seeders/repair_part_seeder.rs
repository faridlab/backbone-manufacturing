//! RepairPart seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RepairPart data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// REPAIRPART SEEDER
// ============================================================================

/// Seeder for repair_part
pub struct SeedRepairPartSeeder;

impl SeedRepairPartSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRepairPartSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRepairPartSeeder {
    fn name(&self) -> &'static str {
        "SeedRepairPartSeeder"
    }

    fn order(&self) -> i32 {
        12
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if manufacturing.repair_parts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM manufacturing.repair_parts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/repair_part_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM manufacturing.repair_parts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
