//! WorkOrder seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial WorkOrder data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// WORKORDER SEEDER
// ============================================================================

/// Seeder for work_order
pub struct SeedWorkOrderSeeder;

impl SeedWorkOrderSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedWorkOrderSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedWorkOrderSeeder {
    fn name(&self) -> &'static str {
        "SeedWorkOrderSeeder"
    }

    fn order(&self) -> i32 {
        6
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if manufacturing.work_orders table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM manufacturing.work_orders")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/work_order_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM manufacturing.work_orders")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

