//! Auth configuration for BomItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<BomItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::BomItemService;
use crate::domain::entity::BomItem;

/// Permission string constants for BomItem operations.
pub mod permissions {
    pub const LIST:        &str = "bom_item:list";
    pub const READ:        &str = "bom_item:read";
    pub const CREATE:      &str = "bom_item:create";
    pub const UPDATE:      &str = "bom_item:update";
    pub const DELETE:      &str = "bom_item:delete";
    pub const RESTORE:     &str = "bom_item:restore";
    pub const TRASH:       &str = "bom_item:trash";
    pub const EMPTY_TRASH: &str = "bom_item:empty_trash";
    pub const BULK_CREATE: &str = "bom_item:bulk_create";
    pub const UPSERT:      &str = "bom_item:upsert";
}

/// Resource policy for BomItem — maps CRUD actions to permission strings.
pub struct BomItemPolicy;

#[async_trait]
impl ResourcePolicy<BomItem> for BomItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &BomItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for BomItem handlers.
///
/// Wraps `BomItemService` and checks `BomItemPolicy` before delegating.
pub type BomItemGuard = PermissionGuard<BomItem>;

/// Service-integrated permission guard for BomItem (for use in generated handlers).
pub type BomItemServiceGuard = ServicePermissionGuard<BomItem, BomItemService, BomItemPolicy>;

// <<< CUSTOM
// END CUSTOM
