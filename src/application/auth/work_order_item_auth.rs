//! Auth configuration for WorkOrderItem handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<WorkOrderItem>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkOrderItemService;
use crate::domain::entity::WorkOrderItem;

/// Permission string constants for WorkOrderItem operations.
pub mod permissions {
    pub const LIST:        &str = "work_order_item:list";
    pub const READ:        &str = "work_order_item:read";
    pub const CREATE:      &str = "work_order_item:create";
    pub const UPDATE:      &str = "work_order_item:update";
    pub const DELETE:      &str = "work_order_item:delete";
    pub const RESTORE:     &str = "work_order_item:restore";
    pub const TRASH:       &str = "work_order_item:trash";
    pub const EMPTY_TRASH: &str = "work_order_item:empty_trash";
    pub const BULK_CREATE: &str = "work_order_item:bulk_create";
    pub const UPSERT:      &str = "work_order_item:upsert";
}

/// Resource policy for WorkOrderItem — maps CRUD actions to permission strings.
pub struct WorkOrderItemPolicy;

#[async_trait]
impl ResourcePolicy<WorkOrderItem> for WorkOrderItemPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &WorkOrderItem,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for WorkOrderItem handlers.
///
/// Wraps `WorkOrderItemService` and checks `WorkOrderItemPolicy` before delegating.
pub type WorkOrderItemGuard = PermissionGuard<WorkOrderItem>;

/// Service-integrated permission guard for WorkOrderItem (for use in generated handlers).
pub type WorkOrderItemServiceGuard = ServicePermissionGuard<WorkOrderItem, WorkOrderItemService, WorkOrderItemPolicy>;

// <<< CUSTOM
// END CUSTOM
