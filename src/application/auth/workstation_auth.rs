//! Auth configuration for Workstation handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Workstation>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkstationService;
use crate::domain::entity::Workstation;

/// Permission string constants for Workstation operations.
pub mod permissions {
    pub const LIST:        &str = "workstation:list";
    pub const READ:        &str = "workstation:read";
    pub const CREATE:      &str = "workstation:create";
    pub const UPDATE:      &str = "workstation:update";
    pub const DELETE:      &str = "workstation:delete";
    pub const RESTORE:     &str = "workstation:restore";
    pub const TRASH:       &str = "workstation:trash";
    pub const EMPTY_TRASH: &str = "workstation:empty_trash";
    pub const BULK_CREATE: &str = "workstation:bulk_create";
    pub const UPSERT:      &str = "workstation:upsert";
}

/// Resource policy for Workstation — maps CRUD actions to permission strings.
pub struct WorkstationPolicy;

#[async_trait]
impl ResourcePolicy<Workstation> for WorkstationPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Workstation,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for Workstation handlers.
///
/// Wraps `WorkstationService` and checks `WorkstationPolicy` before delegating.
pub type WorkstationGuard = PermissionGuard<Workstation>;

/// Service-integrated permission guard for Workstation (for use in generated handlers).
pub type WorkstationServiceGuard = ServicePermissionGuard<Workstation, WorkstationService, WorkstationPolicy>;

// <<< CUSTOM
// END CUSTOM
