//! Domain Entities
//!
//! Generated by metaphor-schema. Do not edit manually.

pub mod workstation;
pub mod workstation_loss;
pub mod workstation_productivity;
pub mod operation;
pub mod bom;
pub mod bom_item;
pub mod bom_operation;
pub mod bom_byproduct;
pub mod bom_subcontractor;
pub mod category_costing_defaults;
pub mod repair_order;
pub mod repair_part;
pub mod repair_tag;
pub mod unbuild_order;
pub mod work_order;
pub mod work_order_item;
pub mod job_card;
pub mod subcontract_mo_link;
pub mod workstation_status;
pub mod operation_status;
pub mod bom_status;
pub mod loss_type;
pub mod bom_type;
pub mod repair_status;
pub mod repair_line_type;
pub mod unbuild_status;
pub mod work_order_state;
pub mod reservation_state;
pub mod job_card_state;

// Re-exports
pub use workstation::Workstation;
pub use workstation::WorkstationBuilder;
pub use workstation::WorkstationId;
pub use workstation_loss::WorkstationLoss;
pub use workstation_loss::WorkstationLossBuilder;
pub use workstation_loss::WorkstationLossId;
pub use workstation_productivity::WorkstationProductivity;
pub use workstation_productivity::WorkstationProductivityBuilder;
pub use workstation_productivity::WorkstationProductivityId;
pub use operation::Operation;
pub use operation::OperationBuilder;
pub use operation::OperationId;
pub use bom::Bom;
pub use bom::BomBuilder;
pub use bom::BomId;
pub use bom_item::BomItem;
pub use bom_item::BomItemBuilder;
pub use bom_item::BomItemId;
pub use bom_operation::BomOperation;
pub use bom_operation::BomOperationBuilder;
pub use bom_operation::BomOperationId;
pub use bom_byproduct::BomByproduct;
pub use bom_byproduct::BomByproductBuilder;
pub use bom_byproduct::BomByproductId;
pub use bom_subcontractor::BomSubcontractor;
pub use bom_subcontractor::BomSubcontractorBuilder;
pub use bom_subcontractor::BomSubcontractorId;
pub use category_costing_defaults::CategoryCostingDefaults;
pub use category_costing_defaults::CategoryCostingDefaultsBuilder;
pub use category_costing_defaults::CategoryCostingDefaultsId;
pub use repair_order::RepairOrder;
pub use repair_order::RepairOrderBuilder;
pub use repair_order::RepairOrderId;
pub use repair_part::RepairPart;
pub use repair_part::RepairPartBuilder;
pub use repair_part::RepairPartId;
pub use repair_tag::RepairTag;
pub use repair_tag::RepairTagBuilder;
pub use repair_tag::RepairTagId;
pub use unbuild_order::UnbuildOrder;
pub use unbuild_order::UnbuildOrderBuilder;
pub use unbuild_order::UnbuildOrderId;
pub use work_order::WorkOrder;
pub use work_order::WorkOrderBuilder;
pub use work_order::WorkOrderId;
pub use work_order_item::WorkOrderItem;
pub use work_order_item::WorkOrderItemBuilder;
pub use work_order_item::WorkOrderItemId;
pub use job_card::JobCard;
pub use job_card::JobCardBuilder;
pub use job_card::JobCardId;
pub use subcontract_mo_link::SubcontractMoLink;
pub use subcontract_mo_link::SubcontractMoLinkBuilder;
pub use subcontract_mo_link::SubcontractMoLinkId;
pub use workstation_status::WorkstationStatus;
pub use operation_status::OperationStatus;
pub use bom_status::BomStatus;
pub use loss_type::LossType;
pub use bom_type::BomType;
pub use repair_status::RepairStatus;
pub use repair_line_type::RepairLineType;
pub use unbuild_status::UnbuildStatus;
pub use work_order_state::WorkOrderState;
pub use reservation_state::ReservationState;
pub use job_card_state::JobCardState;

// ==========================================================================
// Entity Trait
// ==========================================================================

use std::fmt::Debug;

/// Trait for domain entities
///
/// All generated entities implement this trait, providing
/// a common interface for working with domain objects.
pub trait Entity: Debug + Clone {
    /// The type of the entity's unique identifier
    type Id: Debug + Clone + PartialEq;

    /// Get the entity's unique identifier
    fn entity_id(&self) -> &Self::Id;

    /// Get the entity type name
    fn entity_type() -> &'static str;
}

// ==========================================================================
// Audit Metadata
// ==========================================================================

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use uuid::Uuid;
#[cfg(feature = "openapi")]
use utoipa::ToSchema;

/// Audit metadata stored as JSONB in the database
///
/// This struct consolidates audit fields (timestamps and actors) into a single
/// JSONB column for efficient storage and flexible querying.
#[derive(Debug, Clone, Default, Serialize, Deserialize, PartialEq)]
#[cfg_attr(feature = "openapi", derive(ToSchema))]
pub struct AuditMetadata {
    /// Timestamp when the record was created
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was last updated
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_at: Option<DateTime<Utc>>,

    /// Timestamp when the record was soft-deleted
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_at: Option<DateTime<Utc>>,

    /// User ID who created the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub created_by: Option<Uuid>,

    /// User ID who last updated the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub updated_by: Option<Uuid>,

    /// User ID who deleted the record
    #[serde(skip_serializing_if = "Option::is_none")]
    pub deleted_by: Option<Uuid>,
}

impl AuditMetadata {
    /// Create new audit metadata with created_at set to now
    pub fn new() -> Self {
        Self {
            created_at: Some(Utc::now()),
            ..Default::default()
        }
    }

    /// Create with creator ID
    pub fn with_creator(creator_id: Uuid) -> Self {
        Self {
            created_at: Some(Utc::now()),
            created_by: Some(creator_id),
            ..Default::default()
        }
    }

    /// Touch the record (update updated_at)
    pub fn touch(&mut self) {
        self.updated_at = Some(Utc::now());
    }

    /// Touch with updater ID
    pub fn touch_by(&mut self, updater_id: Uuid) {
        self.updated_at = Some(Utc::now());
        self.updated_by = Some(updater_id);
    }

    /// Soft delete the record
    pub fn soft_delete(&mut self) {
        self.deleted_at = Some(Utc::now());
    }

    /// Soft delete with deleter ID
    pub fn soft_delete_by(&mut self, deleter_id: Uuid) {
        self.deleted_at = Some(Utc::now());
        self.deleted_by = Some(deleter_id);
    }

    /// Restore a soft-deleted record
    pub fn restore(&mut self) {
        self.deleted_at = None;
        self.deleted_by = None;
    }

    /// Check if record is deleted
    pub fn is_deleted(&self) -> bool {
        self.deleted_at.is_some()
    }

    /// Check if record is active (not deleted)
    pub fn is_active(&self) -> bool {
        self.deleted_at.is_none()
    }
}
