//! Domain events for RepairPart entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RepairPart;

/// Domain events for RepairPart — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RepairPart>)` variant.
pub type RepairPartEvent = CrudEvent<RepairPart>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RepairPart events.
pub type RepairPartEventPublisher = Arc<dyn CrudEventPublisher<RepairPart> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRepairPartEventPublisher = NoOpCrudEventPublisher<RepairPart>;
