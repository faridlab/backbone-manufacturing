//! Domain events for JobCard entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::JobCard;

/// Domain events for JobCard — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<JobCard>)` variant.
pub type JobCardEvent = CrudEvent<JobCard>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for JobCard events.
pub type JobCardEventPublisher = Arc<dyn CrudEventPublisher<JobCard> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopJobCardEventPublisher = NoOpCrudEventPublisher<JobCard>;
