//! Domain events for RepairTag entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RepairTag;

/// Domain events for RepairTag — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RepairTag>)` variant.
pub type RepairTagEvent = CrudEvent<RepairTag>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RepairTag events.
pub type RepairTagEventPublisher = Arc<dyn CrudEventPublisher<RepairTag> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRepairTagEventPublisher = NoOpCrudEventPublisher<RepairTag>;
