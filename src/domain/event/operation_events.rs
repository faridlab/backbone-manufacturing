//! Domain events for Operation entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Operation;

/// Domain events for Operation — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Operation>)` variant.
pub type OperationEvent = CrudEvent<Operation>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Operation events.
pub type OperationEventPublisher = Arc<dyn CrudEventPublisher<Operation> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopOperationEventPublisher = NoOpCrudEventPublisher<Operation>;
