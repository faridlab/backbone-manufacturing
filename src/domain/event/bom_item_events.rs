//! Domain events for BomItem entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::BomItem;

/// Domain events for BomItem — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<BomItem>)` variant.
pub type BomItemEvent = CrudEvent<BomItem>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for BomItem events.
pub type BomItemEventPublisher = Arc<dyn CrudEventPublisher<BomItem> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBomItemEventPublisher = NoOpCrudEventPublisher<BomItem>;
