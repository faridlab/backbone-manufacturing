//! Domain events for RepairOrder entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RepairOrder;

/// Domain events for RepairOrder — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RepairOrder>)` variant.
pub type RepairOrderEvent = CrudEvent<RepairOrder>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RepairOrder events.
pub type RepairOrderEventPublisher = Arc<dyn CrudEventPublisher<RepairOrder> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRepairOrderEventPublisher = NoOpCrudEventPublisher<RepairOrder>;
