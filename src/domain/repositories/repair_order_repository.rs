//! Repository trait for RepairOrder aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RepairOrder aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RepairOrder, RepairStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairOrderPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RepairOrderPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RepairOrderPaginatedResult {
    pub data: Vec<RepairOrder>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairOrderFilter {
    pub company_id: Option<Uuid>,
    pub repair_number: Option<String>,
    pub item_id: Option<Uuid>,
    pub product_category_id: Option<Uuid>,
    pub status: Option<RepairStatus>,
}

impl RepairOrderFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.repair_number.is_some() || self.item_id.is_some() || self.product_category_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for RepairOrder aggregate
///
/// This trait defines the persistence contract for RepairOrder entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RepairOrderRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new repair_order entity
    async fn save(&self, entity: &RepairOrder) -> Result<RepairOrder>;

    /// Find repair_order by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RepairOrder>>;

    /// Find all repair_order entities
    async fn find_all(&self) -> Result<Vec<RepairOrder>>;

    /// Update repair_order by ID
    async fn update(&self, id: &str, entity: &RepairOrder) -> Result<Option<RepairOrder>>;

    /// Delete repair_order by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List repair_order with pagination
    async fn list(&self, params: RepairOrderPaginationParams) -> Result<RepairOrderPaginatedResult>;

    /// List repair_order with pagination and filters
    async fn list_with_filters(&self, params: RepairOrderPaginationParams, filters: RepairOrderFilter) -> Result<RepairOrderPaginatedResult>;

    /// Count all repair_order entities
    async fn count(&self) -> Result<u64>;

    /// Count repair_order entities matching filters
    async fn count_with_filters(&self, filters: RepairOrderFilter) -> Result<u64>;

    /// Check if repair_order exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete repair_order by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted repair_order by ID
    async fn restore(&self, id: &str) -> Result<Option<RepairOrder>>;

    /// List soft-deleted repair_order entities
    async fn list_deleted(&self, params: RepairOrderPaginationParams) -> Result<RepairOrderPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save repair_order entities
    async fn bulk_save(&self, entities: &[RepairOrder]) -> Result<Vec<RepairOrder>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
