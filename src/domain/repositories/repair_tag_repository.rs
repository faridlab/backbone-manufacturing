//! Repository trait for RepairTag aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RepairTag aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::RepairTag;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairTagPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RepairTagPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RepairTagPaginatedResult {
    pub data: Vec<RepairTag>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairTagFilter {
    pub company_id: Option<Uuid>,
    pub name: Option<String>,
}

impl RepairTagFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.name.is_some()
    }
}

/// Repository trait for RepairTag aggregate
///
/// This trait defines the persistence contract for RepairTag entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RepairTagRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new repair_tag entity
    async fn save(&self, entity: &RepairTag) -> Result<RepairTag>;

    /// Find repair_tag by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RepairTag>>;

    /// Find all repair_tag entities
    async fn find_all(&self) -> Result<Vec<RepairTag>>;

    /// Update repair_tag by ID
    async fn update(&self, id: &str, entity: &RepairTag) -> Result<Option<RepairTag>>;

    /// Delete repair_tag by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List repair_tag with pagination
    async fn list(&self, params: RepairTagPaginationParams) -> Result<RepairTagPaginatedResult>;

    /// List repair_tag with pagination and filters
    async fn list_with_filters(&self, params: RepairTagPaginationParams, filters: RepairTagFilter) -> Result<RepairTagPaginatedResult>;

    /// Count all repair_tag entities
    async fn count(&self) -> Result<u64>;

    /// Count repair_tag entities matching filters
    async fn count_with_filters(&self, filters: RepairTagFilter) -> Result<u64>;

    /// Check if repair_tag exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete repair_tag by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted repair_tag by ID
    async fn restore(&self, id: &str) -> Result<Option<RepairTag>>;

    /// List soft-deleted repair_tag entities
    async fn list_deleted(&self, params: RepairTagPaginationParams) -> Result<RepairTagPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save repair_tag entities
    async fn bulk_save(&self, entities: &[RepairTag]) -> Result<Vec<RepairTag>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
