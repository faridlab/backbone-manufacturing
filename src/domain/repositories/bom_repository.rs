//! Repository trait for Bom aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Bom aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::Bom;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BomPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl BomPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct BomPaginatedResult {
    pub data: Vec<Bom>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BomFilter {
    pub company_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub bom_code: Option<String>,
    pub uom: Option<String>,
    pub currency: Option<String>,
    pub is_active: Option<bool>,
    pub is_default: Option<bool>,
}

impl BomFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.item_id.is_some() || self.bom_code.is_some() || self.uom.is_some() || self.currency.is_some() || self.is_active.is_some() || self.is_default.is_some()
    }
}

/// Repository trait for Bom aggregate
///
/// This trait defines the persistence contract for Bom entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BomRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new bom entity
    async fn save(&self, entity: &Bom) -> Result<Bom>;

    /// Find bom by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Bom>>;

    /// Find all bom entities
    async fn find_all(&self) -> Result<Vec<Bom>>;

    /// Update bom by ID
    async fn update(&self, id: &str, entity: &Bom) -> Result<Option<Bom>>;

    /// Delete bom by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List bom with pagination
    async fn list(&self, params: BomPaginationParams) -> Result<BomPaginatedResult>;

    /// List bom with pagination and filters
    async fn list_with_filters(&self, params: BomPaginationParams, filters: BomFilter) -> Result<BomPaginatedResult>;

    /// Count all bom entities
    async fn count(&self) -> Result<u64>;

    /// Count bom entities matching filters
    async fn count_with_filters(&self, filters: BomFilter) -> Result<u64>;

    /// Check if bom exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete bom by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted bom by ID
    async fn restore(&self, id: &str) -> Result<Option<Bom>>;

    /// List soft-deleted bom entities
    async fn list_deleted(&self, params: BomPaginationParams) -> Result<BomPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save bom entities
    async fn bulk_save(&self, entities: &[Bom]) -> Result<Vec<Bom>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
