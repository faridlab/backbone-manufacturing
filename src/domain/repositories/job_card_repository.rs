//! Repository trait for JobCard aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the JobCard aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{JobCard, JobCardStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct JobCardPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl JobCardPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct JobCardPaginatedResult {
    pub data: Vec<JobCard>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct JobCardFilter {
    pub company_id: Option<Uuid>,
    pub work_order_id: Option<Uuid>,
    pub operation_id: Option<Uuid>,
    pub workstation_id: Option<Uuid>,
    pub status: Option<JobCardStatus>,
}

impl JobCardFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.work_order_id.is_some() || self.operation_id.is_some() || self.workstation_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for JobCard aggregate
///
/// This trait defines the persistence contract for JobCard entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait JobCardRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new job_card entity
    async fn save(&self, entity: &JobCard) -> Result<JobCard>;

    /// Find job_card by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<JobCard>>;

    /// Find all job_card entities
    async fn find_all(&self) -> Result<Vec<JobCard>>;

    /// Update job_card by ID
    async fn update(&self, id: &str, entity: &JobCard) -> Result<Option<JobCard>>;

    /// Delete job_card by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List job_card with pagination
    async fn list(&self, params: JobCardPaginationParams) -> Result<JobCardPaginatedResult>;

    /// List job_card with pagination and filters
    async fn list_with_filters(&self, params: JobCardPaginationParams, filters: JobCardFilter) -> Result<JobCardPaginatedResult>;

    /// Count all job_card entities
    async fn count(&self) -> Result<u64>;

    /// Count job_card entities matching filters
    async fn count_with_filters(&self, filters: JobCardFilter) -> Result<u64>;

    /// Check if job_card exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete job_card by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted job_card by ID
    async fn restore(&self, id: &str) -> Result<Option<JobCard>>;

    /// List soft-deleted job_card entities
    async fn list_deleted(&self, params: JobCardPaginationParams) -> Result<JobCardPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save job_card entities
    async fn bulk_save(&self, entities: &[JobCard]) -> Result<Vec<JobCard>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
