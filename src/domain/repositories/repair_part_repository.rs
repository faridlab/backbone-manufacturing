//! Repository trait for RepairPart aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RepairPart aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RepairPart, RepairLineType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairPartPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RepairPartPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RepairPartPaginatedResult {
    pub data: Vec<RepairPart>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RepairPartFilter {
    pub repair_order_id: Option<Uuid>,
    pub item_id: Option<Uuid>,
    pub warehouse_id: Option<Uuid>,
    pub line_type: Option<RepairLineType>,
}

impl RepairPartFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.repair_order_id.is_some() || self.item_id.is_some() || self.warehouse_id.is_some() || self.line_type.is_some()
    }
}

/// Repository trait for RepairPart aggregate
///
/// This trait defines the persistence contract for RepairPart entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RepairPartRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new repair_part entity
    async fn save(&self, entity: &RepairPart) -> Result<RepairPart>;

    /// Find repair_part by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RepairPart>>;

    /// Find all repair_part entities
    async fn find_all(&self) -> Result<Vec<RepairPart>>;

    /// Update repair_part by ID
    async fn update(&self, id: &str, entity: &RepairPart) -> Result<Option<RepairPart>>;

    /// Delete repair_part by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List repair_part with pagination
    async fn list(&self, params: RepairPartPaginationParams) -> Result<RepairPartPaginatedResult>;

    /// List repair_part with pagination and filters
    async fn list_with_filters(&self, params: RepairPartPaginationParams, filters: RepairPartFilter) -> Result<RepairPartPaginatedResult>;

    /// Count all repair_part entities
    async fn count(&self) -> Result<u64>;

    /// Count repair_part entities matching filters
    async fn count_with_filters(&self, filters: RepairPartFilter) -> Result<u64>;

    /// Check if repair_part exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete repair_part by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted repair_part by ID
    async fn restore(&self, id: &str) -> Result<Option<RepairPart>>;

    /// List soft-deleted repair_part entities
    async fn list_deleted(&self, params: RepairPartPaginationParams) -> Result<RepairPartPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save repair_part entities
    async fn bulk_save(&self, entities: &[RepairPart]) -> Result<Vec<RepairPart>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
