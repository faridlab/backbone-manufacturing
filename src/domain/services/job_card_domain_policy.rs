//! Domain policy for JobCard aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use backbone_core::PermitAllPolicy;
use crate::domain::entity::JobCard;

/// Domain policy for JobCard — permits all operations (no business invariants).
///
/// To add domain rules, replace this alias with a struct implementing
/// `backbone_core::DomainPolicy<JobCard>` in the `// <<< CUSTOM` zone.
pub type JobCardDomainPolicy = PermitAllPolicy<JobCard>;

// <<< CUSTOM
// END CUSTOM
