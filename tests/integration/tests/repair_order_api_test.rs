//! RepairOrder API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the RepairOrder CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// REPAIRORDER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for RepairOrder
pub struct RepairOrderTestData;

impl TestDataGenerator for RepairOrderTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "repair_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "item_id": Uuid::new_v4().to_string(),
            "product_category_id": null,
            "quantity": 0,
            "status": "draft",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "repair_number": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "item_id": Uuid::new_v4().to_string(),
            "product_category_id": null,
            "quantity": 0,
            "status": "draft",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// REPAIRORDER API TEST
// ============================================================================

/// RepairOrder API test suite
pub struct RepairOrderApiTest {
    inner: GenericCrudTest<RepairOrderTestData>,
}

impl RepairOrderApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/repair_orders", "RepairOrder");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, RepairOrderTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for RepairOrderApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_repair_order_crud() {
        let mut test = RepairOrderApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add RepairOrder specific tests here
// <<< CUSTOM TESTS END >>>
