//! JobCard API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the JobCard CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// JOBCARD TEST DATA GENERATOR
// ============================================================================

/// Test data generator for JobCard
pub struct JobCardTestData;

impl TestDataGenerator for JobCardTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": Uuid::new_v4().to_string(),
            "work_order_id": Uuid::new_v4().to_string(),
            "operation_id": Uuid::new_v4().to_string(),
            "workstation_id": Uuid::new_v4().to_string(),
            "total_time_mins": 0,
            "hour_rate": 0,
            "operating_cost": 0,
            "status": "open",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": Uuid::new_v4().to_string(),
            "work_order_id": Uuid::new_v4().to_string(),
            "operation_id": Uuid::new_v4().to_string(),
            "workstation_id": Uuid::new_v4().to_string(),
            "total_time_mins": 0,
            "hour_rate": 0,
            "operating_cost": 0,
            "status": "open",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/work_orders", &super::work_order_api_test::WorkOrderTestData).await {
            deps.push(("work_order_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/operations", &super::operation_api_test::OperationTestData).await {
            deps.push(("operation_id".to_string(), id));
        }
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/workstations", &super::workstation_api_test::WorkstationTestData).await {
            deps.push(("workstation_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// JOBCARD API TEST
// ============================================================================

/// JobCard API test suite
pub struct JobCardApiTest {
    inner: GenericCrudTest<JobCardTestData>,
}

impl JobCardApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/job_cards", "JobCard");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, JobCardTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for JobCardApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_job_card_crud() {
        let mut test = JobCardApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add JobCard specific tests here
// <<< CUSTOM TESTS END >>>
