//! BomByproduct API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the BomByproduct CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// BOMBYPRODUCT TEST DATA GENERATOR
// ============================================================================

/// Test data generator for BomByproduct
pub struct BomByproductTestData;

impl TestDataGenerator for BomByproductTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "company_id": null,
            "bom_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "product_category_id": null,
            "quantity": 0,
            "cost_share": 0,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "company_id": null,
            "bom_id": Uuid::new_v4().to_string(),
            "item_id": Uuid::new_v4().to_string(),
            "product_category_id": null,
            "quantity": 0,
            "cost_share": 0,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/boms", &super::bom_api_test::BomTestData).await {
            deps.push(("bom_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// BOMBYPRODUCT API TEST
// ============================================================================

/// BomByproduct API test suite
pub struct BomByproductApiTest {
    inner: GenericCrudTest<BomByproductTestData>,
}

impl BomByproductApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/bom_byproducts", "BomByproduct");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, BomByproductTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for BomByproductApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_bom_byproduct_crud() {
        let mut test = BomByproductApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add BomByproduct specific tests here
// <<< CUSTOM TESTS END >>>
